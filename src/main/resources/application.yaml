spring:
    application:
        name: caremoa_aftercare
    profiles:
        active: default
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://caremoa-free-mysql.clopnwjywwwu.us-east-2.rds.amazonaws.com:3306/caremoadb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
        username:
        password:
    jpa:
        hibernate:
            ddl-auto: create
        properties:
            hibernate:
                format_sql: true
    mvc:
        pathmatch:
            matching-strategy: ant_path_matcher

server:
    port: 8080

logging:
    level:
        org.hibernate.SQL: debug